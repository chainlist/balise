# Base URL of the balise-sync server used for device pairing and wake signaling.
# Dev defaults to http://localhost:8080 when unset; set this for production builds.
VITE_SYNC_URL=http://localhost:8080
